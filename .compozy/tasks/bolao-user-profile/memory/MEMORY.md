# Workflow Memory

## Current State

- Task 01 (migration) complete. View `user_predictions` created at `0006_user_predictions_view.sql`.

## Shared Decisions

- View `user_predictions` columns: prediction_id, user_id, match_id, predicted_home, predicted_away, points, created_at, home_team, away_team, home_flag, away_flag, group_name, kickoff_at, match_status, actual_home, actual_away. Ordered by created_at DESC.

## Shared Learnings

## Open Risks

## Handoffs
