# graphify
- **graphify** (`.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.

# bracket-config
- **bracket-config** (`.claude/skills/bracket-config/SKILL.md`) - configura confrontos do mata-mata. Trigger: `/bracket-config`
When the user types `/bracket-config`, invoke the Skill tool with `skill: "bracket-config"` before doing anything else.
